import 'package:flutter/material.dart';

import '../../style.dart';

class DigitalChecklistStep extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Placeholder(color: white),
    );
  }
}
