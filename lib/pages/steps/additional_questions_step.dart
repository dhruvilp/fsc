import 'package:flutter/material.dart';

import '../../style.dart';

class AdditionalQuestionsStep extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Placeholder(color: white),
    );
  }
}
